#!/usr/bin/perl
print "// machine-generated by ucf2par ", join(" ",@ARGV), "\n";
print "// edit at your own risk\n";
while (<>) {
	chomp();
	s/ *#.*//;
	next unless (/[^ ]/);
	if (/INST \S+ (\w+)\s+= (\S+);/) {
		print ".$1($2),\n";
	} else {
		print "// unhandled ($_)\n";
	}
}
